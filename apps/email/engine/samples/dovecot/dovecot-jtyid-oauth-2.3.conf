# Managed by OpenShip. The mechanism filter prevents bearer values from ever
# reaching the SQL/password passdb and password values from reaching introspection.
passdb {
    args = PH_DOVECOT_OAUTH2_CONF
    driver = oauth2
    mechanisms = OAUTHBEARER XOAUTH2
}
