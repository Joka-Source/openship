# Managed by OpenShip. Dovecot 2.4 authenticates bearer mechanisms directly from
# this named filter; PLAIN and LOGIN continue through the existing password passdb.
oauth2 {
    client_id = PH_JTYID_DOVECOT_INTROSPECTION_CLIENT_ID
    client_secret = PH_JTYID_DOVECOT_INTROSPECTION_SECRET
    introspection_mode = post
    introspection_url = PH_JTYID_DOVECOT_INTROSPECTION_URL
    username_attribute = email
    active_attribute = active
    active_value = true
}
